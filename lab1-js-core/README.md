## Порядок выполнения лабораторной работы 1

### 1. Перейдите в папку лабораторной работы
```sh
cd lab1-js-core
```

### 2. Создайте свою ветку
```sh
git checkout -b student-{ваш_номер}-lab1
```

### 3. Создайте папку с решениями и скопируйте в неё файл задания с переименованием
```sh
mkdir solutions/student-{ваш_номер}
cp tasks/lab1-tasks.js solutions/student-{ваш_номер}/solution.js
```

### 4. Выполните задания и тесты
```sh
cd solutions/student-{ваш_номер}
node solution.js
```

### 5. Закоммитьте и запушьте изменения
```sh
git add solutions/student-{ваш_номер}/solution.js
git commit -m "Lab 1 solution by student {номер}"
git push origin student-{ваш_номер}-lab1
```

### 6. Сроки сдачи
Дата выдачи: 05.09.2026
Срок ревью: 19.09.2026
Срок сдачи: ближайшая лабораторная работа, после прохождения ревью.

Важно: за работы, сданные после дедлайна, за каждую неделю просрочки снимается 1 балл.

Удачи в выполнении! 🚀

## Теоретическая справка (кратко)

1. **Переменные: `var`, `let`, `const`**
   - `var` — устаревший способ объявления, имеет область видимости функции.
   - `let` — современное объявление для переменных, значение которых может меняться. Имеет блочную область видимости.
   - `const` — современное объявление для констант, значение которых нельзя переопределять (но содержимое объекта или массива, объявленного через `const`, можно изменять). Имеет блочную область видимости.
2. **Типы данных:**
   - Примитивные: Number, String, Boolean, null, undefined, Symbol, BigInt. Передаются по значению.
   - Ссылочные: Object (включая массивы Array, функции Function и даты Date). Передаются по ссылке.
3. **Функции:**
   - Function Declaration: `function sum(a, b) { return a + b; }`
   - Function Expression: `const sum = function(a, b) { return a + b; };`
   - Стрелочные функции (Arrow Functions): `const sum = (a, b) => a + b;` (имеют свои особенности с `this`).
4. **`'use strict'`** — директива, которая переводит интерпретатор в современный, более строгий режим выполнения кода. Обязательна к использованию.
5. **Классы ES6** — синтаксический сахар над прототипным наследованием (вариант создания объектов до ES6). Более удобный и читаемый способ создания конструкторов и работы с прототипами.

```js
class Person {
  constructor(name) { this.name = name; }
  greet() { console.log(`Hello, I'm ${this.name}`); }
}
const person2 = new Person('Bob');
```

6. **Наследование (`extends`)** — ключевое слово `extends` позволяет реализовать наследование классов, где дочерний класс наследует свойства и методы родительского. `super()` используется для вызова конструктора родительского класса.
7. **Инкапсуляция** — принцип, ограничивающий прямой доступ к некоторым компонентам объекта. В современном JavaScript это реализуется через:
   - Публичные поля: `class MyClass { publicField; }`
   - Приватные поля (перед именем ставится `#`): `class MyClass { #privateField; }` — доступны только внутри класса. (Доступно с ES2022 — не используем.)
   - Геттеры (`get`): «функции»-свойства для чтения значения.
   - Сеттеры (`set`): «функции»-свойства для безопасного установления значения с проверками.
8. **Статические методы (`static`)** — методы, принадлежащие самому классу, а не его экземплярам. Вызываются через имя класса.
9. **Замыкание (Closure)** — это способность функции запоминать свою лексическую область видимости (переменные из внешней функции) и иметь доступ к ней даже после того, как внешняя функция завершила выполнение.
10. **Каррирование** — это трансформация функции от многих аргументов в последовательность функций, каждая от одного аргумента.